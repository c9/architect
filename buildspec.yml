version: 0.2

phases:
  install:
    commands:
      - echo Entered the install phase...
      - nvm install 8
      - nvm use 8
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm test
  post_build:
    commands:
      - echo Entered the post_build phase...
      - echo Build completed on `date`